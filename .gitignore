node_modules/
dist/
build/
functions/lib/
.env
.env.*
*.env
*.env.*
serviceAccountKey*.json
*serviceaccount*.json
*.pem
*.key
*.p12
*.pfx
.secrets/
*.log
.DS_Store

# Smoke test generated artifacts (keep tmp-smoke/samples as canonical fixtures)
tmp-smoke/*.json
tmp-smoke/*.txt
tmp-smoke/*.png
tmp-smoke/debug/*.jsonl
tmp-smoke/debug/*.json

# Local tooling/session artifacts (avoid indexing noisy generated bundles/logs)
.debug/
.continue/
.courseforge/
.tmp-release-download-*/

# Release packaging transient artifacts
release/.tmp-*

# MemPalace startup check runtime artifacts
.mempalace/startup-change-check.json
.mempalace/startup-change-check.log.jsonl

# MemPalace per-project files (issue #185)
mempalace.yaml
entities.json

# Keep archival binaries out of git history to avoid Copilot semantic index failures
archive/*.zip
